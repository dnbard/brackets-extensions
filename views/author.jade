extend layout

block content
    h2.center
        span Extensions by&nbsp;
        span.bold.icon-torso #{author}

    - var stars = 0, forks = 0, downloads = 0;
    - for (var i = 0; i < extensions.length; i++) {
    -   forks += (extensions[i].forks || 0);
    -   stars += (extensions[i].stars || 0);
    -   downloads += (extensions[i].totalDownloads || 0)
    - }

    div.overal
        span #{extensions.length} extensions
        span.stars.icon-star-two #{stars}
        span.forks.icon-code-fork #{forks}
        span.downloads.icon-download #{downloads}

    each extension in extensions
        div.query
            div
                a.name(href="/extension/#{extension._id}") #{extension.title}
                span.version #{extension.version}
                if (extension.stars)
                    span.stars.icon-star-two #{extension.stars}
                if (extension.forks)
                    span.forks.icon-code-fork #{extension.forks}
                if (extension.totalDownloads)
                    span.downloads.icon-download #{extension.totalDownloads}
            div.description #{extension.description}
